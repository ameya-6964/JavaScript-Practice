const userName = "Hello User Name Is Ameya and Ameya Is JavaScript Developer"

const firstIndex = userName.indexOf("Ameya"); 
const lastIndex = userName.lastIndexOf("Ameya"); 
console.log(firstIndex); //19
console.log(lastIndex); //23

const includes = userName.includes("User");
console.log(includes); //true


const starts = userName.startsWith("H");
const ends = userName.endsWith("r");
console.log(starts); //true
console.log(ends); //true

